import React, { useState } from 'react';
import { router, usePage } from '@inertiajs/react';
import Icon from './components/Icon';
import Logo from './components/Logo';
import { Avatar, Badge, Button, Field, MetricCard, SectionTitle } from './components/ui';
import { appointments, categories, services, statusConfig } from './data';

const brand = { name: 'Noryvaq', email: 'hola@noryvaq.com', bookingCode: 'NY-2026-4583' };
const navigate = (path) => router.visit(path === '/dashboard' ? '/onboarding' : path);

function App() {
  const { url } = usePage();
  const path = url.split('?')[0];
  if (path.startsWith('/book')) return <BookingPage />;
  if (path.startsWith('/dashboard')) return <DashboardPage />;
  return <LandingPage />;
}

function MarketingHeader() {
  return <header className="marketing-header"><button className="mobile-menu" aria-label="Abrir menú"><Icon name="menu" /></button><button className="logo-button" onClick={() => navigate('/')}><Logo /></button><nav className="main-nav"><a href="#como-funciona">Cómo funciona</a><a href="#negocios">Para negocios</a><a href="#categorias">Categorías</a><a href="#seguridad">Seguridad</a></nav><div className="header-actions"><button className="login-link" onClick={() => navigate('/login')}>Iniciar sesión</button><Button onClick={() => navigate('/onboarding')} iconRight="arrow">Crear mi demo</Button></div></header>;
}

function Hero() {
  return <section className="hero container"><div className="hero-copy"><h1>Más reservas.<br /><span>Un mejor mañana.</span></h1><p>La plataforma que conecta a las personas con los negocios que hacen especiales sus momentos.</p><div className="hero-actions"><Button onClick={() => navigate('/book/luna-beauty-studio')} iconRight="arrow">Explorar reservas</Button><button className="text-action" onClick={() => navigate('/dashboard')}>Haz crecer tu negocio <Icon name="arrow" size={16} /></button></div><div className="hero-trust"><span><Icon name="check" size={16} /> Sin costos ocultos</span><span><Icon name="check" size={16} /> Reservas en segundos</span></div></div><div className="hero-visual"><div className="sun-orb" /><div className="visual-arch arch-one" /><div className="visual-arch arch-two" /><div className="visual-shadow" /><div className="visual-card visual-card-main"><div className="visual-card-top"><span className="tiny-logo"><Logo compact /></span><Badge tone="success" dot>Abierto hoy</Badge></div><h3>Luna Beauty Studio</h3><p>Belleza, bienestar y tiempo para ti.</p><div className="visual-rating"><span>★★★★★</span><small>4.9 · 128 reseñas</small></div><div className="visual-booking"><span><Icon name="calendar" size={15} /> Hoy, 16 de abril</span><span><Icon name="arrow" size={15} /></span></div></div><div className="floating-chip chip-bookings"><span className="chip-icon"><Icon name="calendar" size={15} /></span><span><b>+24%</b><small>reservas este mes</small></span></div><div className="floating-chip chip-rating"><span className="rating-star">★</span><span><b>4.9 / 5</b><small>experiencias reales</small></span></div><div className="visual-caption">Reserva momentos<br /><strong>que sí importan.</strong></div></div></section>;
}

function LandingPage() {
  return <div className="site-shell"><MarketingHeader /><main><Hero /><section className="logo-strip container"><span>Todo lo que necesitas para reservar mejor</span><div><b>24k+</b><small>negocios activos</small></div><div><b>180k+</b><small>momentos reservados</small></div><div><b>4.9/5</b><small>experiencias felices</small></div></section><section id="categorias" className="container section categories-section"><SectionTitle eyebrow="Un mundo de posibilidades" title="Encuentra tu próximo momento." text="Desde una cita de bienestar hasta una cena que querías probar. Todo empieza con una reserva." /><div className="category-grid">{categories.map((category) => <button className="category-card" key={category.label} onClick={() => navigate('/book/luna-beauty-studio')}><span className={`category-icon cat-${category.color}`}><Icon name={category.icon} size={22} /></span><span><strong>{category.label}</strong><small>{category.detail}</small></span><Icon name="arrow" size={17} /></button>)}</div></section><section id="como-funciona" className="steps-section"><div className="container"><SectionTitle eyebrow="Simple desde el primer clic" title="Todo lo que necesitas, en un solo lugar." text="Diseñado para que descubrir, reservar y volver a reservar se sienta natural." /><div className="step-grid"><Step number="01" icon="search" title="Descubre" text="Explora lugares y servicios que encajan contigo." /><Step number="02" icon="calendar" title="Reserva" text="Elige el horario ideal y confirma en segundos." /><Step number="03" icon="heart" title="Disfruta" text="Vive el momento. Nosotros nos ocupamos del resto." /></div></div></section><section id="negocios" className="business-banner container"><div><span className="eyebrow">Para negocios que quieren crecer</span><h2>Tu negocio tiene una historia.<br /><span>Hagamos que suceda.</span></h2><p>Más control, más clientes y más momentos reales. {brand.name} se adapta a tu forma de trabajar.</p><Button onClick={() => navigate('/onboarding')} iconRight="arrow">Crear mi demo</Button></div><div className="business-art"><div className="business-window"><div className="window-line" /><div className="window-line short" /><div className="window-card"><span>Reservas hoy</span><strong>52</strong><small>↑ 12%</small></div></div><div className="business-orb" /></div></section><section id="seguridad" className="container trust-section"><div className="trust-card"><span className="trust-icon"><Icon name="spark" size={20} /></span><div><h3>Una plataforma. Infinitas posibilidades.</h3><p>Segura, flexible y pensada para el mundo real de las reservas.</p></div></div><div className="trust-card"><span className="trust-icon trust-mint"><Icon name="people" size={20} /></span><div><h3>Para cualquier tipo de negocio.</h3><p>Desde una persona hasta equipos y múltiples sedes.</p></div></div><div className="trust-card"><span className="trust-icon trust-blue"><Icon name="chart" size={20} /></span><div><h3>Decisiones con datos.</h3><p>Todo lo que necesitas para crecer con confianza.</p></div></div></section></main><footer className="site-footer container"><Logo /><span>Reserva. Conecta. Haz crecer.</span><div><a href="#como-funciona">Cómo funciona</a><a href="#negocios">Negocios</a><a href={`mailto:${brand.email}`}>Contacto</a></div><small>© 2026 {brand.name}</small></footer></div>;
}

function Step({ number, icon, title, text }) { return <div className="step-card"><span className="step-number">{number}</span><span className="step-icon"><Icon name={icon} size={22} /></span><h3>{title}</h3><p>{text}</p><Icon name="arrow" size={17} className="step-arrow" /></div>; }

function BookingPage() {
  const [step, setStep] = useState(1);
  const [service, setService] = useState(services[0].id);
  const [time, setTime] = useState('10:30');
  const [form, setForm] = useState({ name: '', email: '', phone: '' });
  const selectedService = services.find((item) => item.id === service);
  const update = (key) => (event) => setForm((current) => ({ ...current, [key]: event.target.value }));
  return <div className="booking-shell"><header className="booking-header container"><button className="logo-button" onClick={() => navigate('/')}><Logo /></button><div className="secure-note"><Icon name="check" size={15} /> Reserva segura</div><button className="close-booking" onClick={() => navigate('/')} aria-label="Cerrar"><Icon name="close" size={20} /></button></header><main className="booking-main container"><div className="booking-progress"><button onClick={() => setStep(Math.max(1, step - 1))} disabled={step === 1}><Icon name="arrowLeft" size={17} /></button><div className="progress-copy"><span>Paso {step} de 4</span><strong>{['Elige tu experiencia', 'Encuentra tu horario', 'Tus datos', 'Reserva confirmada'][step - 1]}</strong></div><div className="progress-line"><span style={{ width: `${step * 25}%` }} /></div></div>{step < 4 ? <div className="booking-layout"><aside className="business-summary"><div className="summary-cover" /><Avatar initials="LB" tone="lilac" /><div className="summary-body"><Badge tone="success" dot>Disponible</Badge><h1>Luna Beauty Studio</h1><p>Belleza, bienestar y tiempo para ti.</p><span className="summary-detail"><Icon name="pin" size={15} /> Bogotá · Chapinero</span><span className="summary-detail"><Icon name="star" size={15} /> 4.9 · 128 reseñas</span></div><div className="summary-note"><Icon name="heart" size={17} /><span>Momentos para sentirte bien.</span></div></aside><section className="booking-card">{step === 1 && <><div className="booking-heading"><span className="eyebrow">Luna Beauty Studio</span><h2>¿Qué te gustaría reservar?</h2><p>Elige una experiencia para empezar.</p></div><div className="service-list">{services.map((item) => <button key={item.id} className={`service-option ${service === item.id ? 'selected' : ''}`} onClick={() => setService(item.id)}><span className={`service-option-icon ${item.tone}`}><Icon name={item.icon} size={19} /></span><span><strong>{item.name}</strong><small>{item.meta}</small></span><span className="radio-indicator">{service === item.id && <i />}</span></button>)}</div><div className="booking-footer"><span>¿No sabes qué elegir? <a href={`mailto:${brand.email}`}>Te ayudamos</a></span><Button onClick={() => setStep(2)} iconRight="arrow">Continuar</Button></div></>}{step === 2 && <><div className="booking-heading"><span className="eyebrow">{selectedService.name}</span><h2>Encuentra tu momento.</h2><p>Selecciona un día y un horario que te funcione.</p></div><div className="date-picker"><div className="date-picker-head"><strong>Abril 2026</strong><div><button aria-label="Mes anterior">‹</button><button aria-label="Mes siguiente">›</button></div></div><div className="calendar-week">{['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb', 'Dom'].map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-days">{Array.from({ length: 30 }, (_, index) => <button key={index} className={index === 14 ? 'current' : index === 16 ? 'selected-day' : index < 14 ? 'muted-day' : ''}>{index + 1}</button>)}</div></div><div className="time-selection"><span className="input-label">Horarios disponibles</span><div className="time-grid">{['09:00', '10:30', '12:00', '13:30', '15:00', '16:30'].map((item) => <button key={item} className={time === item ? 'selected' : ''} onClick={() => setTime(item)}>{item}</button>)}</div></div><div className="booking-footer"><span><Icon name="calendar" size={15} /> Jueves, 16 de abril · {time}</span><Button onClick={() => setStep(3)} iconRight="arrow">Continuar</Button></div></>}{step === 3 && <><div className="booking-heading"><span className="eyebrow">Casi listo</span><h2>Cuéntanos sobre ti.</h2><p>Usaremos estos datos para confirmar tu reserva.</p></div><div className="form-grid"><Field label="Nombre completo" placeholder="Ej. Ana Torres" value={form.name} onChange={update('name')} required /><Field label="Correo electrónico" placeholder="ana@ejemplo.com" value={form.email} onChange={update('email')} type="email" required /><Field label="Teléfono" placeholder="+57 300 000 0000" value={form.phone} onChange={update('phone')} type="tel" required /><label className="field field-full"><span>Nota para el negocio <small>(opcional)</small></span><div className="field-control textarea"><textarea placeholder="¿Hay algo que debamos saber?" /></div></label></div><label className="check-field"><input type="checkbox" defaultChecked /><span>Quiero recibir recordatorios y novedades de {brand.name}.</span></label><div className="booking-footer"><span><Icon name="lock" size={15} /> Tus datos están protegidos</span><Button onClick={() => setStep(4)} iconRight="arrow">Confirmar reserva</Button></div></>}</section></div> : <Confirmation service={selectedService} time={time} form={form} />}</main></div>;
}

function Confirmation({ service, time, form }) { return <section className="confirmation"><div className="confirmation-icon"><Icon name="check" size={32} /></div><span className="eyebrow">Tu momento está reservado</span><h1>¡Nos vemos pronto{form.name ? `, ${form.name.split(' ')[0]}` : ''}!</h1><p>Tu reserva en Luna Beauty Studio quedó confirmada. Guarda estos detalles para cuando los necesites.</p><div className="confirmation-details"><div><span>Experiencia</span><strong>{service.name}</strong><small>{service.meta.split('·')[0]}</small></div><div><span>Fecha y hora</span><strong>Jueves, 16 de abril</strong><small>{time} · Bogotá</small></div></div><div className="confirmation-actions"><Button onClick={() => navigate('/')} variant="secondary">Volver al inicio</Button><Button onClick={() => window.print()} icon="calendar">Añadir al calendario</Button></div><small className="confirmation-code">Código de reserva: <strong>{brand.bookingCode}</strong></small></section>; }

function DashboardPage() {
  const [active, setActive] = useState('Inicio');
  const nav = [{ label: 'Inicio', icon: 'home' }, { label: 'Reservas', icon: 'calendar' }, { label: 'Calendario', icon: 'calendar' }, { label: 'Clientes', icon: 'people' }, { label: 'Servicios', icon: 'spark' }, { label: 'Equipo', icon: 'people' }, { label: 'Reportes', icon: 'chart' }, { label: 'Configuración', icon: 'settings' }];
  return <div className="dashboard-shell"><aside className="dashboard-sidebar"><button className="logo-button dashboard-logo" onClick={() => navigate('/')}><Logo /></button><div className="workspace-switch"><Avatar initials="LB" tone="lilac" /><span><small>Workspace</small><strong>Luna Beauty Studio</strong></span><Icon name="chevron" size={15} /></div><nav>{nav.map((item) => <button key={item.label} className={active === item.label ? 'active' : ''} onClick={() => setActive(item.label)}><Icon name={item.icon} size={17} />{item.label}</button>)}</nav><div className="sidebar-bottom"><div className="help-card"><Icon name="spark" size={17} /><strong>¿Necesitas ayuda?</strong><span>Estamos para acompañarte.</span><button>Ir al centro de ayuda <Icon name="arrow" size={14} /></button></div><button className="profile-link"><Avatar initials="MT" tone="blue" /><span><strong>María Torres</strong><small>Administradora</small></span><Icon name="chevron" size={14} /></button></div></aside><main className="dashboard-main"><header className="dashboard-topbar"><button className="dashboard-mobile-menu"><Icon name="menu" /></button><div className="dashboard-breadcrumb"><span>Workspace</span><Icon name="arrow" size={13} /><strong>{active}</strong></div><div className="topbar-actions"><button className="icon-button"><Icon name="search" /></button><button className="icon-button notification"><Icon name="calendar" /><i /></button><Avatar initials="MT" tone="blue" /></div></header><div className="dashboard-content"><div className="dashboard-welcome"><div><span className="eyebrow">Jueves, 16 de abril de 2026</span><h1>Hola, María <span>✦</span></h1><p>Aquí tienes el resumen de tu negocio en {brand.name}.</p></div><Button icon="plus">Nueva reserva</Button></div><div className="metrics-grid"><MetricCard icon="calendar" label="Reservas hoy" value="52" change="↑ 12%" tone="purple" /><MetricCard icon="chart" label="Ingresos del día" value="$3.840" change="↑ 18%" tone="mint" /><MetricCard icon="people" label="Clientes nuevos" value="14" change="↑ 27%" tone="blue" /><MetricCard icon="spark" label="Ocupación" value="86%" change="↑ 6%" tone="orange" /></div><div className="dashboard-grid"><section className="panel calendar-panel"><div className="panel-header"><div><h2>Calendario de reservas</h2><p>Tu agenda, clara y bajo control.</p></div><div className="view-tabs"><button className="active">Día</button><button>Semana</button><button>Mes</button></div></div><div className="calendar-toolbar"><button>‹</button><strong>Abril 2026</strong><button>›</button><button className="today-button">Hoy</button></div><div className="mini-calendar"><div className="calendar-week">{['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb', 'Dom'].map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-days">{Array.from({ length: 30 }, (_, index) => <button key={index} className={index === 15 ? 'selected-day' : index < 15 ? 'muted-day' : ''}>{index + 1}</button>)}</div></div><div className="occupancy"><div className="panel-header"><div><h3>Ocupación por horario</h3><p>Reservas confirmadas hoy</p></div><span className="occupancy-value">86%</span></div><div className="bars">{[25, 45, 35, 68, 52, 86, 62, 48, 31, 58, 42, 68].map((height, i) => <span key={i} style={{ height: `${height}%` }} className={i === 5 ? 'highlight' : ''}><i /></span>)}</div><div className="bar-labels"><small>8h</small><small>10h</small><small>12h</small><small>14h</small><small>16h</small><small>18h</small><small>20h</small></div></div></section><section className="panel agenda-panel"><div className="panel-header"><div><h2>Reservas de hoy <span>(8)</span></h2><p>Jueves, 16 de abril</p></div><button className="link-button">Ver todas <Icon name="arrow" size={14} /></button></div><div className="agenda-list">{appointments.map((item) => <Appointment item={item} key={item.time} />)}</div></section></div><section className="quick-actions"><SectionTitle eyebrow="Tu operación" title="Todo listo para avanzar." /><div className="quick-grid"><Quick icon="plus" title="Crear reserva" text="Agrega un nuevo momento" /><Quick icon="people" title="Ver clientes" text="Conoce a tu comunidad" /><Quick icon="spark" title="Gestionar servicios" text="Ajusta tu oferta" /><Quick icon="chart" title="Revisar reportes" text="Decide con datos" /></div></section></div><MobileDashboardNav active={active} setActive={setActive} /></main></div>;
}

function Appointment({ item }) { const config = statusConfig[item.status]; return <button className="appointment-row"><span className="appointment-time">{item.time}</span><Avatar initials={item.initials} tone={item.tone} /><span className="appointment-info"><strong>{item.name}</strong><small>{item.service}</small></span><Badge tone={config.tone}>{config.label}</Badge><Icon name="chevron" size={15} className="appointment-chevron" /></button>; }
function Quick({ icon, title, text }) { return <button className="quick-card"><span><Icon name={icon} size={19} /></span><strong>{title}</strong><small>{text}</small><Icon name="arrow" size={15} /></button>; }
function MobileDashboardNav({ active, setActive }) { return <nav className="mobile-dashboard-nav"><button className={active === 'Inicio' ? 'active' : ''} onClick={() => setActive('Inicio')}><Icon name="home" size={19} /><span>Inicio</span></button><button className={active === 'Reservas' ? 'active' : ''} onClick={() => setActive('Reservas')}><Icon name="calendar" size={19} /><span>Reservas</span></button><button className="mobile-add"><Icon name="plus" size={21} /></button><button onClick={() => setActive('Clientes')} className={active === 'Clientes' ? 'active' : ''}><Icon name="people" size={19} /><span>Clientes</span></button><button onClick={() => setActive('Configuración')} className={active === 'Configuración' ? 'active' : ''}><Icon name="settings" size={19} /><span>Más</span></button></nav>; }

export default App;
